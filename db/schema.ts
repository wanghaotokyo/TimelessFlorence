import { sqliteTable, text, integer, index } from 'drizzle-orm/sqlite-core';
export const users = sqliteTable('users', { id: text('id').primaryKey(), name: text('name').notNull(), email: text('email').notNull() });
export const sessions = sqliteTable('sessions', { hash: text('hash').primaryKey(), userId: text('user_id').notNull(), expires: integer('expires').notNull() });
export const guides = sqliteTable('guides', { id: text('id').primaryKey(), userId: text('user_id').notNull(), title: text('title').notNull(), data: text('data').notNull(), created: text('created').notNull(), version: integer('version').notNull().default(1), deleted: integer('deleted').notNull().default(0) }, t => [index('idx_guides_user_created').on(t.userId, t.created)]);
export const jobs = sqliteTable('jobs', { id: text('id').primaryKey(), userId: text('user_id').notNull(), kind: text('kind').notNull(), state: text('state').notNull(), payload: text('payload').notNull(), providerId: text('provider_id'), research: text('research'), sources: text('sources'), result: text('result'), error: text('error'), created: integer('created').notNull(), updated: integer('updated').notNull() }, t => [index('idx_jobs_user_created').on(t.userId, t.created)]);
